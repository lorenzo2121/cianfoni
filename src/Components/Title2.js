import React from 'react';
import '../Styles/title2.css'
const Title = ({ text }) => {
    return (
        <div className="title2">
            <h1>{text}</h1>
        </div>
    );
};

export default Title;
